# Lombok 全局配置
# 将字段上的 @Qualifier 复制到 Lombok 生成的构造参数上，
# 使 @RequiredArgsConstructor 配合 @Qualifier 字段时 Spring 构造器注入仍按限定名解析。
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
