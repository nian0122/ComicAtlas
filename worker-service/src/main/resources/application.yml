server:
  port: 8020

spring:
  application:
    name: comic-worker-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
      config:
        import-check:
          enabled: false
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASS:guest}

worker:
  manga-root: ${MANGA_ROOT:/manga}
  temp-dir: ${MANGA_ROOT:/manga}/temp
  metadata-dir: ${MANGA_ROOT:/manga}/metadata
  aria2c-path: ${ARIA2C_PATH:worker-service/aria2-1.37.0-win-64bit-build1/aria2c.exe}
  torrent:
    peer-detect-timeout: 30
    min-speed-threshold: 10240
    speed-check-duration: 300
  proxy:
    host: ${PROXY_HOST:127.0.0.1}
    port: ${PROXY_PORT:7897}
    socks-port: ${SOCKS_PORT:7897}
