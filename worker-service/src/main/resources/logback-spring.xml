<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_DIR" value="${LOG_DIR:-logs}" />
    <property name="LOG_FILE" value="${LOG_DIR}/worker.log" />
    <property name="LOG_ARCHIVE" value="${LOG_DIR}/worker-%d{yyyy-MM-dd}-%i.log.gz" />

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_ARCHIVE}</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 自定义包 DEBUG: MQ 消息收发、下载流程、文件处理 -->
    <logger name="com.comicatlas.worker" level="DEBUG" />

    <!-- RabbitMQ 连接/消费详情 -->
    <logger name="org.springframework.amqp" level="INFO" />
    <logger name="org.springframework.amqp.rabbit.listener" level="DEBUG" />

    <!-- HTTP 请求详情（e-hentai API 调用） -->
    <logger name="jdk.internal.net.http" level="INFO" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
