=== COMMITS 8ed0f53..e1edbef === 
e1edbef feat(阅读端): 漫画库移除状态筛选下拉
 
=== STAT === 
 frontend/e2e/library-filter.spec.ts        | 10 ++++++++++
 frontend/src/views/reading/LibraryPage.vue | 15 +--------------
 2 files changed, 11 insertions(+), 14 deletions(-)
 
=== FULL DIFF U10 === 
diff --git a/frontend/e2e/library-filter.spec.ts b/frontend/e2e/library-filter.spec.ts
index 9e34194..83c48ba 100644
--- a/frontend/e2e/library-filter.spec.ts
+++ b/frontend/e2e/library-filter.spec.ts
@@ -65,10 +65,20 @@ async function mockRoutes(page: Page, captured: CapturedParams) {
 test('漫画库请求恒带 status=READY', async ({ page }) => {
   const captured: CapturedParams = { status: [], category: [] }
   await mockRoutes(page, captured)
 
   await page.goto('/library')
   await expect(page.locator('.comic-poster').first()).toBeVisible({ timeout: 10000 })
 
   expect(captured.status.length).toBeGreaterThan(0)
   expect(captured.status.every((s) => s === 'READY')).toBe(true)
 })
+
+test('工具栏不含状态筛选下拉', async ({ page }) => {
+  const captured: CapturedParams = { status: [], category: [] }
+  await mockRoutes(page, captured)
+
+  await page.goto('/library')
+  await expect(page.locator('.comic-poster').first()).toBeVisible({ timeout: 10000 })
+
+  await expect(page.locator('.status-select')).toHaveCount(0)
+})
diff --git a/frontend/src/views/reading/LibraryPage.vue b/frontend/src/views/reading/LibraryPage.vue
index 4f3ae10..4786972 100644
--- a/frontend/src/views/reading/LibraryPage.vue
+++ b/frontend/src/views/reading/LibraryPage.vue
@@ -23,30 +23,20 @@
               <option value="updatedAt">最近更新</option>
               <option value="title">标题</option>
               <option value="pageCount">页数</option>
               <option value="lastReadTime">最近阅读</option>
             </select>
           </div>
         </div>
 
         <!-- 移动端第二行：筛选 chips 横向滚动 -->
         <div class="toolbar-filters">
-          <div class="filter-select status-select">
-            <select v-model="statusFilter" @change="onSearch">
-              <option value="">全部状态</option>
-              <option value="READY">已就绪</option>
-              <option value="IMPORTING">导入中</option>
-              <option value="PENDING">等待中</option>
-              <option value="FAILED">失败</option>
-            </select>
-          </div>
-
           <div class="filter-select tag-filter">
             <el-select
               v-model="selectedTags"
               multiple
               collapse-tags
               placeholder="筛选标签"
               class="tag-select"
               @change="onSearch"
             >
               <el-option
@@ -125,21 +115,20 @@ import { useComicStore } from '@/stores/comic-store'
 import { tagApi } from '@/services/management'
 import { useBreakpoint, BREAKPOINTS } from '@/composables/useBreakpoint'
 import ComicPoster from '@/components/reading/comic/ComicPoster.vue'
 import { toPosterStatus } from '@/components/reading/comic/poster-status'
 import type { ComicListQuery, ComicListVO, TagDTO } from '@/types'
 
 const router = useRouter()
 const store = useComicStore()
 
 const keyword = ref('')
-const statusFilter = ref('')
 const sort = ref<NonNullable<ComicListQuery['sort']>>('createdAt')
 const selectedTags = ref<string[]>([])
 const tagMode = ref<'AND' | 'OR'>('OR')
 const allTags = ref<TagDTO[]>([])
 
 // 响应式视口宽度（resize 防抖更新，组件卸载时自动清理监听）
 const viewportWidth = useBreakpoint()
 
 // 海报尺寸随断点响应式推导（替代原先读取一次视口宽度、手动挂 resize 监听的写法）
 const posterSize = computed<'sm' | 'md' | 'lg'>(() => {
@@ -165,21 +154,20 @@ async function loadTags() {
     const res = await tagApi.list()
     allTags.value = (res.data as TagDTO[]) || []
   } catch (err: unknown) {
     allTags.value = []
   }
 }
 
 function onSearch() {
   store.search({
     keyword: keyword.value || undefined,
-    status: statusFilter.value || undefined,
     sort: sort.value,
     tags: selectedTags.value.length > 0 ? selectedTags.value : undefined,
     tagMode: selectedTags.value.length > 1 ? tagMode.value : undefined,
   })
 }
 
 function onPageChange(page: number) {
   store.updateQuery({ page })
   store.fetchList()
 }
@@ -313,29 +301,28 @@ onMounted(() => {
 
 .tag-filter :deep(.el-select__tags) {
   color: var(--text-primary);
 }
 
 .tag-mode-filter {
   min-width: 110px;
 }
 
 /* 桌面端（>768px）：包装层不参与布局，控件直接平铺进 toolbar，
- * 并用 order 恢复原有控件顺序：搜索 → 状态 → 排序 → 标签 → 标签模式 */
+ * 并用 order 恢复原有控件顺序：搜索 → 分类 → 排序 → 标签 → 标签模式 */
 @media (min-width: 769px) {
   .toolbar-main,
   .toolbar-filters {
     display: contents;
   }
 
   .search-input { order: 1; }
-  .status-select { order: 2; }
   .sort-select { order: 3; }
   .tag-filter { order: 4; }
   .tag-mode-filter { order: 5; }
 }
 
 .comic-section {
   display: flex;
   flex-direction: column;
   gap: var(--space-xl);
   padding-bottom: var(--space-xl);
